using System.Collections;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using UnityEngine;

namespace YovanStankovic
{
    public class WeekTwoActivities : MonoBehaviour
    {
        private string favouriteGame = "Super Mario Galaxy 2";
        private int hoursPlayed = 500;
        private float costofGame = 80f; 
        
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("My favourite game is " + favouriteGame + "!!!!!");
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
